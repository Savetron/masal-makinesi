{
  "name": "@masal-makinesi/prompts",
  "version": "1.0.0",
  "description": "Prompt templates and guard rules",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint --ignore-pattern 'dist' '**/*.{ts,tsx}'",
    "test": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vite-tsconfig-paths": "^5.1.4"
  }
}